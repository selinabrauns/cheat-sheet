var React = require('react');var Button = require('../atoms/button');module.exports = React.createClass({        getInitialState: function(){            return {                    showList: false            }        },        render: function() {                var self = this;                var list = this.props.listItems.map(function(item){                        return <li key={item.key}><a href='#'>{item.name}</a></li>                });                var dropdown = <ul>{list}</ul>                return <div className='dropdown' key={this.props.key}><Button btnLable={this.props.btnLable}                                                className={this.props.className} whenClicked={self.handleClick}/>                                   {self.state.showList ? dropdown : null}                          </div>        },        handleClick: function(){                this.setState({                        showList: !this.state.showList                });        } });